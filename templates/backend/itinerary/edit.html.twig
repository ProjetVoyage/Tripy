{% extends 'base.html.twig' %}

{% block title %} Modifier l'itinéraire {% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('map') }}
{% endblock %}

{% block body %}
    <h1> Modifier l'itinéraire </h1>

    {{ include('backend/itinerary/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('itinerary_index' , {'id': travel.id} ) }}">back to list</a>

    <div id="map"></div>

    {{ include('backend/itinerary/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('map') }}
{% endblock %}