{% extends 'base.html.twig' %}

{% block title %}Travel{% endblock %}

{% block body %}
    <h1>Travel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ travel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ travel.name }}</td>

            </tr>
             <tr>
                <th>Traveler</th>    
                <td>
                    {% for traveler in travelers %}
                  
                    <a href="{{ path('traveler_show', {'id': traveler.id}) }}">  {{ traveler.username }}</a>

                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('travel_index') }}">back to list</a>

    <a href="{{ path('travel_edit', {'id': travel.id}) }}">edit</a>

    {{ include('backend/travel/_delete_form.html.twig') }}
{% endblock %}
