{% extends 'base.html.twig' %}

{% block title %}Traveler{% endblock %}

{% block body %}
    <h1>Traveler</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traveler.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('traveler_index') }}">back to list</a>

    <a href="{{ path('traveler_edit', {'id': traveler.id}) }}">edit</a>

    {{ include('backend/traveler/_delete_form.html.twig') }}
{% endblock %}
